name: CD

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag to publish (for example: v1.2.0)"
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10.30.1

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Resolve release metadata
        id: release
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            tag="${{ inputs.tag }}"
          else
            tag="${GITHUB_REF_NAME}"
          fi

          if [[ ! "${tag}" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Expected tag in vX.Y.Z format, got: ${tag}" >&2
            exit 1
          fi

          version="${tag#v}"
          package_version="$(node -p "require('./package.json').version")"
          package_name="$(node -p "require('./package.json').name")"
          release_notes="docs/releases/${version}.md"

          if [[ "${package_version}" != "${version}" ]]; then
            echo "Tag ${tag} does not match package.json version ${package_version}" >&2
            exit 1
          fi

          if [[ ! -f "${release_notes}" ]]; then
            echo "Missing release notes file: ${release_notes}" >&2
            exit 1
          fi

          {
            echo "tag=${tag}"
            echo "version=${version}"
            echo "package_name=${package_name}"
            echo "release_notes=${release_notes}"
          } >> "${GITHUB_OUTPUT}"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check npm publish credentials
        shell: bash
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          set -euo pipefail

          if [[ -z "${NODE_AUTH_TOKEN:-}" ]]; then
            echo "Missing required repository secret: NPM_TOKEN" >&2
            echo "Add an npm automation token in GitHub repository settings before rerunning CD." >&2
            exit 1
          fi

          npm whoami

      - name: Check npm package version
        id: npm_version
        shell: bash
        run: |
          set -euo pipefail

          if npm view "${{ steps.release.outputs.package_name }}@${{ steps.release.outputs.version }}" version >/dev/null 2>&1; then
            echo "Version ${{ steps.release.outputs.version }} is already published to npm"
            echo "exists=true" >> "${GITHUB_OUTPUT}"
          else
            echo "Version ${{ steps.release.outputs.version }} is not published to npm yet"
            echo "exists=false" >> "${GITHUB_OUTPUT}"
          fi

      - name: Check GitHub Release
        id: github_release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail

          if gh release view "${{ steps.release.outputs.tag }}" >/dev/null 2>&1; then
            echo "GitHub Release ${{ steps.release.outputs.tag }} already exists"
            echo "exists=true" >> "${GITHUB_OUTPUT}"
          else
            echo "GitHub Release ${{ steps.release.outputs.tag }} does not exist yet"
            echo "exists=false" >> "${GITHUB_OUTPUT}"
          fi

      - name: Build
        run: pnpm run build

      - name: Run local integration tests
        run: pnpm run test:proxy-local

      - name: Generate coverage report
        run: pnpm run test:coverage

      - name: Verify publish artifact
        run: env npm_config_cache=/tmp/claude-proxy-npm-cache npm pack --dry-run

      - name: Publish package to npm
        if: steps.npm_version.outputs.exists != 'true'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm publish

      - name: Publish GitHub Release
        if: steps.github_release.outputs.exists != 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.release.outputs.tag }}
          name: ${{ steps.release.outputs.tag }}
          body_path: ${{ steps.release.outputs.release_notes }}
          draft: false
          prerelease: false
          generate_release_notes: false
