# `@sunflower0305/claude-proxy` v1.1.3

Patch release of `claude-proxy`, published as `@sunflower0305/claude-proxy`.

## Highlights

- npm README now includes a Changelog section linking to `CHANGELOG.md` and GitHub Releases
- `CHANGELOG.md` is now included in the published npm package

## Installation

```bash
npx @sunflower0305/claude-proxy
```

or:

```bash
npm install -g @sunflower0305/claude-proxy
claude-proxy
```

## Verification Summary

Local release gates to complete for `v1.1.3` before publication:

- local TypeScript build completes successfully
- local integration test suite passes
- coverage generation completes successfully
- `npm pack --dry-run` confirms the published artifact includes `dist/`, `README.md`, `CHANGELOG.md`, `LICENSE`, and `.env.example`

Post-publish verification plan:

- confirm the `CD` workflow runs successfully for tag `v1.1.3`
- verify npm publication completes and the `latest` dist-tag points to `1.1.3`
- confirm the npm package README includes the Changelog section

## Source

- GitHub: <https://github.com/sunflower0305/claude-proxy>
- npm: <https://www.npmjs.com/package/@sunflower0305/claude-proxy>
